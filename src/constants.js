// Shared scene dimensions (world units). Both appearances build against the same layout so
// the ring physics, particle spawn areas and camera framing stay identical.
export const POT_RADIUS = 4.4;
export const RING_RADIUS = 2.9;
export const DUMPLING_SCALE = 0.92;
export const FRAME_RADIUS = 5.4; // world units visible from the centre along the shorter canvas side
export const HANDLE_ANGLE = Math.PI / 5; // handle towards the bottom-right of the screen
